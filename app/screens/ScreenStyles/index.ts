import userStyle from "./UserScreen.style"
import postStyle from "./PostScreen.style"

/**
 * Consolidates screen-specific styles into a single export.
 * Use descriptive keys to clarify the screen each style belongs to.
 */
const styles = {
  userScreenStyles: userStyle,
  postScreenStyles: postStyle,
}

export default styles
